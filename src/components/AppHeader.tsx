import type { ReactNode } from "react";

import { logoutAction } from "@/app/actions/auth";
import type { PoUser } from "@/lib/auth/session";

/**
 * 머리말.
 *
 * 서버 컴포넌트다 — "use client" 를 붙이지 않는다. 로그아웃은 서버 액션을
 * 부르는 평범한 <form> 이라 자바스크립트 없이도 동작한다. 사내망에서
 * 스크립트가 늦게 붙는 동안 눌러도 제대로 나가진다.
 *
 * 「통합 로그인으로」 버튼은 포털 앱 런처로 간다. 로그아웃과 다르다 —
 * 세션을 끊지 않으므로 돌아오면 그대로 들어와 있다.
 *
 * ── 🔴 이 줄에 무엇이 들어가나 ─────────────────────────────────────────
 * 이 줄은 다섯을 담는다:
 *
 *   시스템 이름 · 오가기 메뉴바 · **화면 메뉴** · 사용자명 · 나가는 단추 둘
 *
 * 🔴 셋째(화면 메뉴)가 2026-09-21 에 들어왔다. 「내자 정리 · 견적서 · 작업
 * 비용」 알약 단추 셋이다(components/AppNav.tsx). 전에는 이 머리말 **아래**
 * 제 테두리를 가진 띠였는데, 사용자 지정으로 이 줄 안에 넣었다 — "PO의
 * 메뉴바는 휴가관리에서 메뉴바 안에 버튼들이 있었던 것 처럼 만들어줘".
 * 서비스 메뉴바를 머리말 **위**에서 **안**으로 넣은 2026-09-18 결정과 같은
 * 방향이다(화면 맨 위가 두 층이면 답답하고 본문이 한 줄만큼 줄어든다).
 *
 * 🔴 폰(360px)에서 그 다섯이 한 줄에 다 들어가지 않는다. 실제 폭(글자 14px,
 * 한글은 한 자가 1em):
 *
 *   바깥 여백 px-4 를 뺀 속폭                                    328px
 *   ─────────────────────────────────────────────────────────────────
 *   시스템 이름 "DSS PO / 내자" (16px)                         ~104px
 *   사용자명 3글자                                               ~42px
 *   「통합 로그인으로」  글자 102 + px-3 24 + 테두리 2          ~128px
 *   「로그아웃」          글자  56 + px-3 24 + 테두리 2          ~82px
 *   메뉴 단추(아이콘만)                                          ~59px
 *   화면 메뉴 단추 셋 84 + 66 + 84 + gap-1 두 번                ~242px
 *   ─────────────────────────────────────────────────────────────────
 *
 * 그래서 **둘**을 함께 쓴다.
 *
 * ① 🔴 이 줄은 `flex-wrap` 이다(2026-09-21, 화면 메뉴를 넣으면서 켰다).
 *    줄바꿈이 없으면 넘칠 때 줄이 바뀌는 것이 아니라 flex 가 칸을 min-content
 *    까지 눌러 **단추 안에서 글자를 접는다** — "통합 / 로그인으로", "내자 /
 *    정리". 켜 두면 덩이 하나가 통째로 아랫줄로 갈 뿐이다. 폰에서는 실제로
 *    두 줄이 된다: 「메뉴 단추 + 화면 메뉴」(59+12+242=313 ≤ 328) / 「나가는
 *    단추 둘」(222). 휴가 관리(dss-leave)가 같은 자리에서 같은 답을 냈다.
 *
 * ② 폰에서는 **글자만 알려 주는 두 덩이**를 그래도 눈에서 감춘다 — 시스템
 *    이름과 사용자명이다. 둘 다 눌러서 갈 곳이 없는 정보고, 감추면 ~104 + 42
 *    + 여백 = ~174px 이 돌아와 줄 수가 셋이 아니라 둘로 끝난다. (개선요청
 *    dss-improvements 가 같은 셈으로 같은 답을 냈다. 계측기 njlee 는 폰에서
 *    이미 여러 줄이라 그쪽은 이름을 되돌렸다 — 저장소마다 답이 다른 이유가
 *    이것이다.)
 *
 * 🔴 「통합 로그인으로」와 「로그아웃」은 감추지 않는다 — 그 둘 말고 이
 * 사이트를 떠날 길이 없다. 폰에서 가장 넓은 자리를 먹는 것이 그 둘이지만,
 * 나가는 길을 화면 밖으로 밀어내서는 안 된다.
 *
 * 🔴 감추는 방식은 `sr-only` 이지 `hidden` 이 **아니다** — 마크업에 그대로
 * 남아 화면 낭독기는 여전히 "DSS PO / 내자" 와 사용자명을 읽는다. 게다가
 * sr-only 는 position:absolute 라 flex 항목에서 통째로 빠진다 — 폭뿐 아니라
 * 앞뒤 여백까지 함께 메뉴바로 간다.
 *
 * 🔴 기준점은 `md:`(=768px) 하나다 — @dss/ui 가 **단추**에서 이름을 감추는
 * 기준과 같은 값이라야 그 사이 폭에서 「이름은 없는데 단추는 글자」인 어정쩡한
 * 상태가 생기지 않는다(service-menu.css 의 `not all and (min-width: 768px)`).
 *
 * 🔴 펼친 목록은 단추 아래로 **떠서**(position: absolute, z-index 50) 그려져
 * 이 머리말 밖으로 나온다. 그래서 이 <header> 와 그 조상(= (app)/layout.tsx 의
 * div, body, html)에 `overflow: hidden` 을 걸면 **목록이 잘려 아무것도 고를 수
 * 없게 된다.** 지금은 한 곳도 없고, 쌓임 맥락을 새로 만드는 것(z-index·
 * transform·filter·isolation)도 없다 — 그대로 두어야 한다.
 *
 * 🔴 역할 뱃지를 달지 않는다. 이 시스템의 역할은 A/S 와 같은 5역할이고, 거기에
 * 무엇을 할 수 있는지는 role_permissions 표가 정한다 — 역할 이름 하나를 머리말에
 * 띄우면 그것이 곧 권한인 것처럼 읽힌다. 기능별 판정은 화면을 옮겨 오는 조각에서
 * 그 화면과 함께 가져온다(lib/auth/guards.ts 머리말).
 */
export function AppHeader({
  user,
  portalUrl,
  serviceMenu = null,
  screenNav = null,
  notificationBell = null,
}: {
  user: PoUser;
  portalUrl: string;
  /**
   * 사내 시스템 오가기 목록(@dss/ui 의 ServiceMenuBar). (app)/layout.tsx 가
   * 서버에서 만들어 내려보내고, 이 머리말이 **이름과 사용자명 사이**에 그린다.
   *
   * 조각이 아니라 **다 그려진 노드**를 받는 이유: 이 파일이 @dss/ui 도, 목록을
   * 어디서 구하는지도 몰라야 한다. 그리는 자리만 여기가 정한다(아래
   * `shrink-0 mr-auto` 한 겹 — 그것이 이 줄의 선을 지키는 장치다).
   * 목록이 비면 그 조각이 스스로 null 이라 빈 칸만 남고 아무것도 안 보인다.
   */
  serviceMenu?: ReactNode;
  /**
   * 이 사이트 **안**의 화면 메뉴(components/AppNav.tsx 의 알약 단추 셋).
   * (app)/layout.tsx 가 권한으로 거른 뒤 만들어 내려보내고, 이 머리말이
   * **오가기 메뉴바 다음, 사용자명 앞**에 그린다.
   *
   * serviceMenu 와 같은 이유로 조각이 아니라 **다 그려진 노드**를 받는다 —
   * 이 파일이 권한도, 메뉴 목록을 어디서 구하는지도 몰라야 한다. 그리는 자리만
   * 여기가 정한다. 항목이 없으면 그 조각이 스스로 null 이라 빈 자리도 남지
   * 않는다(권한이 전혀 없는 사람에게 빈 띠만 보이면 고장으로 읽힌다).
   *
   * 🔴 **바깥 것과 안엣 것의 차례**: 오가기 메뉴바(다른 시스템으로 건너가는
   * 길)가 먼저고 화면 메뉴(이 사이트 안)가 뒤다. 넓은 데서 좁은 데로 좁혀
   * 들어가는 차례라야 눈이 헤매지 않는다 — 휴가 관리도 같은 차례다.
   */
  screenNav?: ReactNode;
  /**
   * 알림 종(@dss/ui 의 NotificationBell). 위 둘과 **같은 방식**으로 받는다 —
   * 다 그려진 노드다. 이 파일은 @dss/ui 도, 알림을 어디서 구하는지도 모른다.
   *
   * (app)/layout.tsx 가 `<Suspense>` 로 감싼 조각을 내려보내므로, 포털이
   * 느려도 이 머리말은 기다리지 않는다 — 종만 나중에 흘러 들어온다.
   *
   * 🔴 그릴 알림이 없으면 그 조각이 **스스로 null** 이다(@dss/ui 의 판단 —
   * 눌러도 아무것도 없는 단추를 머리말에 남기지 않는다). 그래서 래퍼 <div> 로
   * 감싸지 않고 **flex 항목으로 그대로** 둔다: 감싸면 알림이 없을 때도 빈
   * 항목과 그 앞 여백(gap-3 12px)이 남아 머리말이 예전과 달라진다.
   */
  notificationBell?: ReactNode;
}) {
  return (
    <header className="border-b border-slate-200 bg-white">
      {/*
        🔴 폭 제한이 없다 — 창 끝까지 쓴다(2026-09-21). 한때 본문·메뉴와 함께
        `mx-auto max-w-[1100px]` 이었는데, 본문의 그 못을 뽑으면서 여기도 같이
        뽑았다. 본문만 풀면 넓은 화면에서 시스템 이름과 나가는 단추는 가운데
        1100px 띠 안에 남고 표만 창 끝까지 뻗어 **왼쪽 끝이 어긋난다.**
        바깥 틀 셋(머리말 · 화면 메뉴 · 본문)은 같은 폭·같은 좌우 여백이라야
        한 장으로 보인다((app)/layout.tsx 의 <main> 주석).

        🔴 좌우 여백의 폰 값 `px-4` 는 **바꾸지 않는다** — 이 파일 머리말의
        360px 폭 계산이 속폭 328(=360-16*2)을 전제로 하고, 시험이 그 셈을 그대로
        못 박아 둔다(service-menu-wiring.test.ts). 768px 이상에서만 24px 로
        벌린다.
      */}
      {/*
        🔴 `flex-wrap` 이고 `justify-between` 이 **없다**(2026-09-21). 전에는
        한 줄짜리 justify-between 이었는데, 화면 메뉴가 이 줄로 들어오면서 둘 다
        바뀌었다:

        · `flex-wrap` — 폰에서 넘칠 때 단추 안 글자가 접히는 대신 덩이가 통째로
          아랫줄로 간다(이 파일 머리말 ①의 셈).
        · `justify-between` 대신 **나가는 묶음의 `ml-auto`** — 항목이 넷이 되면
          justify-between 은 남는 자리를 **항목 사이마다** 고르게 나눠, 이름 ·
          메뉴 단추 · 화면 메뉴가 줄 전체에 흩뿌려진다. 자동 여백은 한 자리에만
          걸 수 있어서 「왼쪽에 붙은 셋 + 오른쪽 끝의 나가는 묶음」이 된다.
          게다가 줄이 바뀐 뒤에는 justify-between 이 아랫줄에서 또 따로 일해
          단추 둘이 양 끝으로 벌어진다.

        여백은 `gap-x-3 gap-y-2` 다 — 가로는 12px(전 gap-4=16 에서 좁혔다.
        덩이가 하나 늘어 폰 한 줄의 셈이 빡빡해졌다), 줄이 바뀌었을 때 위아래는
        8px 로 붙여 머리말이 불필요하게 두꺼워지지 않게 한다.
      */}
      <div className="flex w-full flex-wrap items-center gap-x-3 gap-y-2 px-4 py-3 md:px-6">
        {/*
          시스템 이름. 폰(<768px)에서는 **눈에서만** 감춘다 — 왜인지는 이 파일
          머리말의 폭 계산에 있다. 낭독기에는 그대로 남고, 본문 맨 위에는 화면
          이름이 큰 글씨로 따로 있다.
        */}
        <h1 className="sr-only text-base font-semibold text-slate-900 md:not-sr-only">
          DSS PO / 내자
        </h1>

        {/*
          사내 시스템 오가기 목록. 이름 다음, 사용자명 앞 — 넓은 화면에서
          통째로 비어 있던 가운데 자리다.

          🔴 `shrink-0` 이다. 기준 폭을 **제 내용 폭**으로 둔다 — 폰 59px
          (아이콘만), 768px 이상 113px 남짓(아이콘 + 이름). 그리는 것은
          `white-space: nowrap` 인 **단추 하나**라 줄어들지 못하므로, 기준 폭
          0 인 칸(`min-w-0 flex-1`)에 넣어 두면 자리가 모자랄 때 단추가 제 칸
          밖으로 삐져나와 사용자명·나가는 단추와 겹친다.

          🔴 한때 여기 `mr-auto` 가 함께 있었다. 그때 이 줄은 `justify-between`
          이었고, 항목이 셋이라 가운데 것이 남는 자리를 먹지 않으면 이 단추가
          **줄 한가운데로 밀렸다** — `mr-auto` 가 그것을 이름 옆에 붙들어 둔
          장치였다. 2026-09-21 에 justify-between 을 걷고 남는 자리를 **나가는
          묶음의 `ml-auto`** 하나가 가져가게 바꾸면서 그 까닭이 사라졌다.
          여기 `mr-auto` 를 되돌리면 그 자동 여백이 남는 자리를 먼저 다 먹어
          **화면 메뉴가 오른쪽 끝의 나가는 단추에 가서 붙는다.**
        */}
        <div className="shrink-0">{serviceMenu}</div>

        {/*
          이 사이트 안의 화면 메뉴. 오가기 메뉴바 **다음**, 사용자명 **앞** —
          바깥(다른 시스템)에서 안(이 사이트의 화면)으로 좁혀 들어가는 차례다.
          감싸는 <div> 를 두지 않는다: 내려오는 것이 이미 <nav> 하나라
          (components/AppNav.tsx) 그것이 그대로 이 줄의 flex 항목이 된다.
        */}
        {screenNav}

        {/*
          나가는 길이 모인 오른쪽 묶음.

          🔴 `ml-auto` 가 이 줄의 자동 여백 **하나뿐인 자리**다 — 남는 자리를
          전부 가져가 왼쪽 셋(이름 · 메뉴 단추 · 화면 메뉴)과 이 묶음을 양쪽
          끝으로 갈라 놓는다. justify-between 을 대신하는 것이 이 한 낱말이다.

          🔴 `flex-wrap` 과 `justify-end` 도 짝이다. 바깥 줄이 이 묶음을 통째로
          아랫줄로 내려 주더라도 묶음 **안**은 한 줄이라 거기서 다시 넘칠 수
          있고(폰에서 이 묶음만 222px), 그때 줄바꿈이 없으면 또 단추 안에서
          글자가 접힌다. `justify-end` 는 줄이 바뀐 뒤에만 일한다 — 아랫줄로
          내려간 단추가 왼쪽에 붙어 윗줄과 어긋나 보이지 않게 한다.
        */}
        <div className="ml-auto flex flex-wrap items-center justify-end gap-3 text-sm">
          {/*
            누구로 들어와 있는지. 폰에서는 **눈에서만** 감춘다(위와 같은 이유) —
            눌러서 갈 곳이 없는 정보라 좁은 화면에서 가장 먼저 내줄 자리다.
            낭독기는 그대로 읽으므로 「내가 누구로 들어와 있나」를 확인할 길은
            남는다.
          */}
          <span className="sr-only text-slate-600 md:not-sr-only">{user.name}</span>

          {/* 🔴 아래 둘은 폰에서도 감추지 않는다 — 이 사이트를 떠나는 유일한 길이다. */}
          <a
            href={portalUrl}
            className="rounded-md border border-slate-300 px-3 py-1.5 text-slate-700 hover:bg-slate-50"
          >
            통합 로그인으로
          </a>

          <form action={logoutAction}>
            <button
              type="submit"
              className="rounded-md border border-slate-300 px-3 py-1.5 text-slate-700 hover:bg-slate-50"
            >
              로그아웃
            </button>
          </form>

          {/*
            알림 종. 🔴 **줄의 맨 오른쪽 끝**이다 — 펼친 목록은 종에 오른쪽
            끝을 맞춰 왼쪽으로 펼쳐지므로(@dss/ui 의
            `.dss-bell__list { right: 0 }`), 가운데쯤에 앉으면 폰에서 목록
            왼쪽이 화면 밖으로 잘린다. 목록 폭은 `min(20rem, 100vw - 2rem)` 이라
            폰(360px)에서 320px 이고, 종이 이 묶음의 끝에 있어야 그 320px 이
            화면 안에 그대로 들어온다(이 묶음은 `ml-auto justify-end` 라 어느
            줄에 놓이든 오른쪽 끝에 붙는다).

            🔴 폰(360px)의 폭 셈에 더해지는 것 — 종은 40px 이다(@dss/ui 는
            손가락 화면에서 36 → 40 으로 키운다):

              알림이 없을 때  종이 **아예 안 그려진다** → 예전 그대로
              알림이 있을 때  이 묶음 222 + gap-3 12 + 종 40 = 274 ≤ 328 ✔
                              (첫 줄의 「메뉴 단추 + 화면 메뉴」 313 은 그대로)

            래퍼 <div> 로 감싸지 않는다: 알림이 없으면 조각이 스스로 null 이라
            항목 자체가 사라져야 하는데, 감싸면 빈 항목과 그 앞 여백이 남는다.

            인쇄에는 나오지 않는다 — @dss/ui 의 종 CSS 가
            `@media print { display: none }` 을 제 안에 갖고 있다.
          */}
          {notificationBell}
        </div>
      </div>
    </header>
  );
}
