/**
 * ============================================================================
 * 주간보고 납입 예정 건 — `입고 요청일` 한 칸을 정하는 규칙
 * ============================================================================
 * weekly-report.ts · weekly-report-goal.ts 와 같은 자리의 파일이다: **DB 도
 * React 도 들어오지 않는 순수 함수만** 둔다.
 *
 * 이 표의 여덟 칸 중 일곱은 따로 정할 것이 없다 — 여섯은 수리 건에서 그대로
 * 따라오고(schema/weekly-report-deliveries.ts 헤더), 비고는 사람이 친 값
 * 그대로다. 판단이 필요한 칸은 **`입고 요청일` 하나**뿐이라, 이 파일에 든 것도
 * 그 하나다. 쓰이지 않을 함수를 미리 만들어 두지 않는다.
 *
 * 주 계산(mondayOfDateOnly · weekStartOfKst · weekLabel)과 상자 안 차례
 * (sortWeeklyReportGoals)는 **weekly-report-goal.ts 에 이미 있다.** 납입 예정
 * 건은 금주 목표와 **같은 주 고르개를 쓰므로**(승인된 결정) 같은 함수를 그대로
 * 부른다 — 두 벌을 만들면 한쪽만 고쳐지는 날이 오고, 그날 두 상자가 서로 다른
 * 주를 가리킨다.
 * ============================================================================
 */

/**
 * 그 수리 건의 **`입고 요청일`** — 붙어 있는 납기요청일 중 **가장 이른 하루**.
 *
 * 재료는 내자 정리에서 온다: `domestic_orders`(is_deleted = false) → 그 줄들에
 * 딸린 `domestic_order_due_dates.due_date` 전부. **한 건에 내자 줄이 여럿이고
 * 줄 하나에 날짜가 또 여럿**이므로(분할 발주 · 분할 납품), 이 함수가 받는 것은
 * 언제나 **여러 줄에 걸친 날짜를 통틀어 모은 한 묶음**이다. 줄마다 min 을 낸 뒤
 * 다시 고르지 않는다 — 중간에 한 번 접으면 "어느 줄의 날짜인가"라는 뜻이 슬쩍
 * 끼어들지만, 여기서 답하려는 물음은 "이 건에서 가장 이른 날이 언제인가"다.
 *
 * ── 이미 지난 날짜라도 그것이 가장 이르면 그것을 적는다 ─────────────────
 * 승인된 결정이다. 오늘을 기준으로 지난 날짜를 걸러 내면, 납기를 넘긴 건의
 * 요청일이 화면에서 조용히 사라져 **가장 급한 줄이 가장 늦어 보이는** 표가
 * 된다. 늦은 것은 늦은 대로 보여야 사람이 그것을 먼저 처리한다. 그래서 이
 * 함수는 오늘이 언제인지 알지 못하고, 알 필요도 없다 — 문자 그대로 min 이다.
 *
 * ── 문자열 비교로 고른다 ────────────────────────────────────────────────
 * "YYYY-MM-DD" 는 자리수가 고정된 0 채움 형식이라 사전순과 날짜순이 같다.
 * `new Date()` 로 바꿔 견주지 않는 것은 이 저장소가 실제로 겪은 함정 때문이다 —
 * `date` 컬럼에서 온 날짜를 파싱하면 UTC 자정이 되어 한국에서 하루 밀린다
 * (domain/date-only.ts 헤더). 날짜는 끝까지 문자열로 다닌다.
 *
 * 날짜가 하나도 없으면 **null** 이다 — 화면의 그 칸은 빈칸으로 남는다. 내자
 * 줄이 아예 없는 건, 줄은 있어도 납기요청일을 아직 안 적은 건이 실제로 있고,
 * 그것은 오류가 아니라 "아직 정해지지 않았다"는 사실이다.
 */
export function pickEarliestDueDate(dueDates: readonly string[]): string | null {
  let earliest: string | null = null;
  for (const dueDate of dueDates) {
    if (earliest === null || dueDate < earliest) earliest = dueDate;
  }
  return earliest;
}
