/**
 * ============================================================================
 * 🔴 엑셀 전용 견적서는 **이 사이트에서 받을 수 없다** — 붙일 칸 자체가 없다
 * ============================================================================
 * 엑셀 전용 견적서(quotes.is_excel_only)의 문서는 **사람이 손으로 만든 엑셀**이다.
 * A/S 관리 시스템의 받기 통로는 그래서 앱 양식을 채우지 않고 **첨부 칸에 지금 붙어
 * 있는 파일을 그대로 흘려보낸다**(저쪽 `api/quotes/[id]/xlsx/route.ts` 의 5-2 갈래와
 * 형제 파일 `download-source.ts`).
 *
 * 🔴 **이 사이트에는 첨부 칸이 아예 없다**(조각 3d 가 가져온다). 엑셀을 올릴 길이
 * 없으므로 엑셀 전용 견적서의 「붙인 엑셀」은 여기서 **영영 존재하지 않는다** — 그
 * 갈래를 그대로 옮기면 첨부 사슬 2,144줄(attachment-allowlist · attachment-category ·
 * queries/attachments · attachment-path · attachment-download-policy · local-fs-adapter ·
 * storage-adapter)이 전부 따라오고, 그래도 돌려줄 파일은 없다.
 *
 * 그래서 이 사이트는 **거절한다**(조각 3c-2 결정):
 *   · 받기 통로(GET /api/quotes/{id}/xlsx)는 501 과 아래 문장으로 답한다.
 *   · 목록의 그 줄에는 [견적서 받기] 링크 대신 같은 문장을 곁말로 보인다.
 * 🔴 **누를 수 있는데 404 가 되는 것보다 낫다.** A/S 와 동작이 다른 것은 정당하다 —
 * 그쪽에는 붙인 엑셀이 있고 이쪽에는 없다.
 *
 * ── 🔴 `canRenderQuoteDocument` 와 **별개의 조건**이다 ──────────────────
 * 그 판정(domain/quote-document-support.ts)은 「앱 양식이 있는 종류인가」를 묻고,
 * **엑셀 전용이면 언제나 참**을 돌려준다 — 그 파일 머리말이 까닭을 적어 두었다(앱
 * 양식을 쓰지 않으므로 잘못 나갈 일이 없고, 「여기서까지 막으면 이미 되는 일을 못 하게
 * 만드는 것」). 저쪽에서는 실제로 되는 일이라 그 말이 맞다. 🔴 그래서 그 판정을 고치지
 * 않고 **조건을 하나 더 둔다** — 둘은 서로 다른 것을 묻는다:
 *
 *     canRenderQuoteDocument(quote)   앱 양식이 있는 종류인가      (종류의 문제)
 *     quote.isExcelOnly               이 사이트가 내줄 파일이 있나 (첨부 칸의 문제)
 *
 * 조각 3d 가 첨부 칸을 가져오는 날 이 파일은 사라지고, 그 자리에 A/S 의
 * `download-source.ts` 갈래가 들어온다.
 *
 * ── 문장이 한 곳이다 ────────────────────────────────────────────────────
 * 화면(목록의 곁말)과 서버(통로의 JSON)가 **같은 문장**을 쓴다. 두 벌이면 한쪽만
 * 고쳐지는 날이 오고, 그때 사람은 화면에서 본 말과 다른 말을 통로에서 듣는다
 * (A/S 의 `QUOTE_EXCEL_MISSING_MESSAGE` · `QUOTE_DOCUMENT_UNSUPPORTED_MESSAGE` 와
 * 같은 방식이다). 🔴 이 파일은 순수한 글자 하나뿐이라 **클라이언트 묶음에 실려도
 * 안전하다** — 목록(`"use client"`)이 들여온다.
 * ============================================================================
 */

/**
 * 엑셀 전용 견적서를 받으려 했을 때 사람에게 보이는 문장.
 *
 * 🔴 「오류」가 아니라 **아직 안 되는 일**이라고 말한다 — 사람은 자기가 무엇을 잘못했는지
 * 찾게 되고, 그 답은 「아무것도 잘못하지 않았다」이기 때문이다(그 판정 파일의 거절 문장과
 * 같은 판단). 까닭(첨부 칸이 없다)과 지금 할 수 있는 일(A/S 에서 받는다)까지 담는다.
 *
 * 🔴 조각 번호를 적지 않는다 — 사람에게는 뜻이 없는 말이고, 순서가 바뀌면 틀린 말이 된다.
 */
export const QUOTE_EXCEL_ONLY_DOWNLOAD_MESSAGE =
  "엑셀 전용 견적서는 붙여 둔 엑셀 파일이 곧 견적서입니다 — 이 사이트에는 아직 파일을 붙이는 칸이 없어 내려받을 수 없습니다. 그 칸이 오기 전까지는 A/S 관리 시스템에서 받아 주세요.";

/** 목록의 그 줄에 링크 대신 서는 짧은 글자. 긴 까닭은 위 문장이 곁말(title)로 붙는다. */
export const QUOTE_EXCEL_ONLY_ROW_LABEL = "엑셀 전용";
