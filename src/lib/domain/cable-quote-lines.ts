/**
 * ============================================================================
 * 🔴 케이블 견적서 한 장에 담기는 줄 수 — **임시로 여기 적어 둔 숫자다** (3b-1)
 * ============================================================================
 * 케이블 양식은 품목 자리가 **아홉**이고 줄을 늘리지 않는다(A/S 의
 * `lib/xlsx/cable-quote-template.ts` 의 `CABLE_QUOTE_ITEM_ROWS` —
 * `[27, 29, 31, 33, 35, 37, 39, 41, 43]`, 한 줄씩 띄운 자리다). 열째 줄을 넣으면
 * 채우개가 자르지 않고 **던진다.** 그래서 편집 화면이 **줄을 더하는 자리에서
 * 미리 막는다** — 저장한 뒤 [견적서 받기]에서야 실패하면 늦다.
 *
 * ── 🔴 왜 저쪽 상수를 부르지 않는가 ────────────────────────────────────
 * 저쪽 상수는 채우개 파일에 있고, 그 파일은 `node:fs` · `node:zlib` 를 끌고 와
 * **클라이언트 묶음에 들어올 수 없다.** 그래서 A/S 에서도 **서버 컴포넌트인
 * 페이지가 읽어 폼에 넘긴다**(QuoteEditForm 의 `cableMaxLines` 프롭). 이 사이트에는
 * 그 채우개가 아직 없다 — **엑셀 사슬은 조각 3c** 다.
 *
 * 🔴 **조각 3c 가 오면 이 파일을 지우고** 페이지가
 * `lib/xlsx/cable-quote-template.ts` 의 `CABLE_QUOTE_MAX_LINES` 를 넘기게 한다.
 * 숫자가 두 벌인 상태를 오래 두지 않는다 — 양식이 바뀌는 날 한쪽만 고쳐지고,
 * 그때 증상은 「화면은 넣어 주는데 문서를 만들다 던지는」 견적서다.
 *
 * 🔴 프롭(`cableMaxLines`)은 **그대로 두었다.** 폼 안에 숫자를 박아 두면 3c 가
 * 고칠 자리가 폼 안쪽으로 들어가고, 그러면 A/S 의 폼과 글자가 갈린다.
 * ============================================================================
 */

/** 품목 줄 + 설명 줄을 **합쳐서** 아홉. A/S `CABLE_QUOTE_ITEM_ROWS.length` 와 같은 값이다. */
export const CABLE_QUOTE_MAX_LINES = 9;
